name: sats-receiver-test-venv
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - coverage
  - gnuradio>=3.10.10
  - gnuradio-satellites>=5.5
  - pip:
      - -r requirements.txt
